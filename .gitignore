# Local tooling (engine binaries etc.) — not committed, machine-specific
.tools/

# Salidas del benchmark del spike (CSV/PNG regenerables) — los hallazgos
# quedan documentados en docs/sprint-02.md, no hace falta versionar los dumps
game/benchmark_results/
game3d/benchmark_results/

# Godot 4+ specific ignores
.godot/
*.translation
export.cfg
export_credentials.cfg
export_presets.cfg

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp

# Future GDExtension build artifacts (C++ / godot-rust) — Sprint 2+
bin/
obj/
target/
*.o
*.obj
*.so
*.dll
*.dylib
CMakeFiles/
CMakeCache.txt
